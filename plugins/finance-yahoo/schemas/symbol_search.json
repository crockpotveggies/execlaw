{
    "type": "object",
    "properties": {
        "query": {
            "type": "string",
            "minLength": 1,
            "description": "Free-text search. Matches company names (`apple`), tickers (`AAPL`), and fuzzy variants. Returns mixed asset types — `type_disp` tells you whether each hit is Equity, ETF, Index, Cryptocurrency, Currency, or Mutual Fund."
        },
        "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 20,
            "description": "Max results to return (default 10)."
        }
    },
    "required": ["query"],
    "additionalProperties": false
}
