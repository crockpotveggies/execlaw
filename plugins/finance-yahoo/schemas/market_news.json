{
    "type": "object",
    "properties": {
        "query": {
            "type": "string",
            "minLength": 1,
            "description": "Topic, ticker, or company name to find news for (e.g. `AAPL`, `inflation`, `OpenAI`)."
        },
        "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 20,
            "description": "Max headlines to return (default 10)."
        }
    },
    "required": ["query"],
    "additionalProperties": false
}
