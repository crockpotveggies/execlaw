[package]
name = "plugin-identity-local-address-book"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "In-tree reference identity provider — resolves identifiers against a JSON contact list."
publish = false

[[bin]]
name = "identity-local-address-book"
path = "src/main.rs"

[dependencies]
serde.workspace = true
serde_json.workspace = true
