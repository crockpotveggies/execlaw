{
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "Place name to resolve (e.g. \"Reykjavík\", \"San Francisco\", \"Mount Fuji\"). Min 2 characters."
        },
        "count": {
            "type": "integer",
            "minimum": 1,
            "maximum": 20,
            "description": "Maximum results to return (1-20, default 5)."
        },
        "language": {
            "type": "string",
            "description": "ISO-639-1 language code for localized place names (default \"en\")."
        }
    },
    "required": ["name"],
    "additionalProperties": false
}
